# Global workspace instructions

Applies to every repo under `~/workspace`. Per-repo `CLAUDE.md` wins on conflict.

## Branching and PRs

1. Never commit to the default branch. Branch off it and work there.
2. When done, push and open a PR with `gh pr create`.
3. After opening, monitor the PR until merged or closed: watch checks, fix failures, address review comments.
